---
name: wanggan-editing
description: 为口播视频按逐字时间戳添加网感缩放、普通烧录字幕和逐条累积清单，自动把重点和正面信息放大、把负面信息缩小并露出黑边，把枚举条目移到画面上方持续展示，生成可逐字调整、启用字幕和热重载的本地审查网页，确认后导出最终 MP4，适用于网感剪辑、重点放大、负面缩小、普通字幕烧录、清单枚举、逐字稿驱动缩放、AI 初剪和人机协同视频剪辑
---

# 网感剪辑

把语义判断和视频处理分开执行

- 由调用本 skill 的 Agent 阅读逐字稿并决定效果范围
- 由脚本校验时间戳、生成审查网页、保存人工修改并渲染视频
- 保持原视频和原逐字稿只读
- 在用户确认前只生成实时预览，不渲染最终视频

## 工作流程

1. 运行环境检查

```powershell
node scripts/wanggan.mjs doctor
```

2. 查找字幕来源并创建任务目录

- 必须找到扁平 JSON 数组形式的字级逐字稿，每项包含 `text`、`start`、`end`
- 在视频目录、逐字稿目录和两者的直接相邻目录检查分句 SRT
- 优先选择与视频同名的 `<视频名>.srt`，其次选择 `<视频名>.captions.srt`
- 如果找到多个无法确定对应关系的 SRT，先向用户确认，不得自行混用
- SRT 只负责普通字幕的分句和文字，不能代替必需的字级 JSON
- 没有 SRT 时允许省略 `--subtitle`，脚本会按停顿、标点和长度从字级 JSON 自动分句

```powershell
node scripts/wanggan.mjs init --video "<视频路径>" --transcript "<逐字稿路径>" --subtitle "<可选 SRT 路径>" --project "<任务目录>"
```

3. 读取逐字稿并设计效果

在生成 `effects.json` 前，必须完整阅读 [效果选择规范](references/effect-selection-spec.md)

按以下顺序判断

1. 先划分完整语义块
2. 判断是否值得添加效果
3. 判断重点或负面
4. 判断短促或长效果
5. 判断是否同时添加大字号、亮颜色
6. 检查范围边界、停顿、SRT 关系和效果密度
7. 写入 `effects.json` 并运行导入校验

不得只根据“不、没有、失败、重要”等单个词语机械分类

4. 把全部 AI 决定写成一个 JSON 文件，格式见 [effects-format.md](references/effects-format.md)

5. 原子式导入全部效果，任意一条不合法时整批拒绝

```powershell
node scripts/wanggan.mjs import --project "<任务目录>" --input "<AI 效果 JSON>"
```

需要单独增加一条效果时运行

```powershell
node scripts/wanggan.mjs add --project "<任务目录>" --start 12.467 --end 13.967 --effect-type short_emphasis
```

需要添加清单枚举式结构时，调用 Agent 识别同一组有序条目，把条目范围和屏幕文案写入 `overlays.json` v2，再原子式导入

```powershell
node scripts/wanggan.mjs import-overlays --project "<任务目录>" --input "<AI 覆盖层 JSON>"
```

- 类型固定为 `progressive_list`
- 每个条目提交连续的 `start_word_index`、`end_word_index` 和可选 `display_text`
- 脚本重新计算 `start`、`end` 与 `source_text`
- 播到一个条目时在画面上方增加该条目，已经出现的条目继续保留
- 条目本身播放期间隐藏普通底部字幕
- 条目之间的补充说明继续使用普通底部字幕，上方已有条目保持显示
- 最后一条结束时整组清单立即消失

6. 启动独立审查网页

```powershell
node scripts/wanggan.mjs serve --project "<任务目录>" --port 8911
```

把命令输出的本地地址交给用户，在网页中逐字调整范围和固定效果类型

网页右侧平级显示五个效果选项

- 短促重点、短促负面、长重点、长负面作用于主视频图层，同一段文字只保留其中一种视频效果
- 大字号、亮颜色作用于字幕图层，可以和任意一种视频效果同时选择
- 点击任一选项后立即写入 `effects.json` 并热重载，保存成功后自动取消当前文字选择
- 点击逐字稿空白、视频区域、右侧面板或网页其他非字块区域时，立即取消当前文字选择
- “大字号、亮颜色”直接切换样式，不做字号或颜色过渡

网页右侧“字幕”区域提供一个小型切换按钮，关闭时显示“启用”，开启时显示“撤下”，点击即保存并热重载

- 开启时网页实时预览普通字幕，最终渲染把同一字幕烧录进 MP4
- 撤下时网页和最终 MP4 都不显示字幕
- 点击视频里的字幕区块后显示白色矩形，拖动区块可调整整条字幕轨道的位置
- 白色矩形右下角提供圆形缩放按钮，拖动可调整整条字幕轨道的宽度和高度
- 拖动和缩放期间字幕区块必须完整位于视频画面内，松开后立即写入 `overlays.json` 并热重载

网页右侧“清单”区域用于审查结构化覆盖层

- Agent 导入的清单会直接显示条目原话范围和屏幕文案
- 点击条目编号跳到对应时间并选中逐字范围
- 支持修改屏幕文案、用当前选择替换范围、追加条目和删除条目
- 清单组支持启用、撤下和删除
- 视频中的清单区块支持独立拖动和缩放，位置使用屏幕比例坐标
- 所有清单修改点击即写入 `overlays.json` 并热重载

网页顶部提供“保存工程”按钮

- 画面效果和字幕状态仍然点击即写入工程文件
- “保存工程”额外保存当前播放位置、逐字稿选择范围和保存时间
- 下次使用同一工程目录启动审查台时，自动恢复人工编辑内容、字幕状态、播放位置和选择范围
- 保存工程只写入工程目录，不渲染视频、不覆盖输入素材

7. 仅在用户明确确认后渲染

```powershell
node scripts/wanggan.mjs render --project "<任务目录>"
```

网页中的“确认并生成成片”按钮执行同一渲染流程

8. 检查任务状态

```powershell
node scripts/wanggan.mjs status --project "<任务目录>"
```

## 硬性规则

- 只接受 JSON 数组形式的逐字稿，每项必须包含 `text`、`start`、`end`
- 字级 JSON 始终是必需输入，SRT 只是可选的普通字幕分句来源
- SRT 必须按 UTF-8 读取，字幕条目时间有序、不重叠且不能超过视频时长
- 普通字幕和结构化覆盖层配置使用 [overlays-format.md](references/overlays-format.md) 的 `overlays.json` v2 格式
- 读取旧 v1 文件时保留字幕状态并补成空的 `timed_overlays`，下次保存写成 v2
- 普通字幕默认关闭，只有用户在审查网页明确启用后才参与预览和最终渲染
- 普通字幕位于画面缩放层上方，不跟随底层视频缩放或裁切
- 结构化清单同样位于画面缩放层上方，不跟随底层视频缩放或裁切
- 所有普通字幕条目共享 `overlays.json` 中同一个字幕区块，位置或尺寸修改必须应用于全部字幕条目
- 字幕区块拖动和缩放必须限制在画面内，网页预限制和服务端校验缺一不可
- 清单区块拖动和缩放必须限制在画面内，内容高度超过清单区域时拒绝保存
- 清单条目时间必须精确对应逐字稿词语边界，按词序排列且不能重叠
- 同一个清单最多 8 个条目，启用的清单组时间不能互相重叠
- 清单条目出现后保持到整组结束，整组结束时间固定为最后一条的结束时间
- 普通字幕只在清单条目自身的播放区间隐藏，条目间空档必须恢复
- 预览必须使用服务端返回的 `playbackOverlays` 和避让后的 `playbackCaptions`
- 编辑现场使用 [editor-state-format.md](references/editor-state-format.md) 的 `editor-state.json` v1 格式
- “保存工程”必须保留已有 `effects.json` 和 `overlays.json`，只更新编辑现场文件
- 重新启动同一个工程时必须恢复人工效果、字幕状态、播放位置和逐字稿选择范围
- 时间单位统一使用秒
- 效果时间必须精确对应逐字稿中的词语边界
- 相邻效果可以连接，同一 `target` 上的重叠效果必须拒绝，不同 `target` 允许使用同一词语范围
- 词序相邻且 `effect_type` 相同的效果在预览和渲染时连续播放，不在逐字时间空隙中恢复 `100%`
- 预览必须使用服务端返回的 `playbackEffects`，与渲染器共享同一套连续区间计算，禁止网页自行复制一套合并逻辑
- 网页检测到导出引擎版本不匹配时必须禁止导出并提示重启，不能允许预览逻辑与导出逻辑不同步
- 选中文字后，短促重点、短促负面、长重点、长负面、大字号亮颜色五个平级选项和清除画面效果必须点击即保存并热重载，不再设置独立保存按钮
- 四种画面效果的缩放程度固定，字幕强调的字号和颜色固定，全部不接受 `params`
- 短促效果进入和退出时直接硬切
- 长效果在所选词语的 `start` 到 `end` 之间线性缩放，结束时间点直接硬切回 `100%`
- 放大时居中裁切，缩小时居中补纯黑边
- 审查网页使用稳定画布显示视频帧，播放器控件不得进入画面缩放层
- 人工修改 AI 决定后保留 `source: ai`，同时写入 `human_modified: true`
- 不覆盖输入视频、输入逐字稿或已经存在的输出文件
- 不自动调用任何外部模型或要求额外模型密钥

## 文件职责

- `project.json` 保存只读输入路径、视频信息和输出路径
- `effects.json` 使用 v3 接口，是 AI 与人类共同编辑的目标图层效果真源，旧 v2 文件读取后按画面效果兼容
- `overlays.json` 使用 v2 接口，保存普通字幕、结构化清单、各自屏幕坐标和基础样式
- 字幕局部强调保存在 `effects.json`，通过 `target: overlay.captions` 引用 `overlays.json` 中的字幕轨道
- `editor-state.json` 使用 v1 接口，保存当前播放位置、逐字稿选择范围和最后保存时间
- `render-overlays.ass` 是从普通字幕和结构化覆盖层共同编译出的渲染中间文件，不反向覆盖字幕或逐字稿来源
- `render-status.json` 保存出片状态和错误信息
- `render-filter.txt` 保存最终 FFmpeg 滤镜，便于检查预览与出片是否一致
