{
  "schema_version": 1,
  "kind": "effect_type",
  "id": "base.pop",
  "operator": "core.transition.scale-opacity",
  "requires_capabilities": ["transform.scale", "style.opacity"],
  "timing_models": ["item_enter", "asset_enter"],
  "writes_channels": ["transform.scale.entry", "style.opacity.entry"],
  "overlap_policy": "exclusive-per-channel",
  "config_schema": { "type": "object", "additionalProperties": false },
  "ui": { "label": "轻微弹出", "toggle_label": "入场动画" }
}
